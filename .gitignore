# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri 构建产物
src-tauri/target
src-tauri/binaries
src-tauri/gen
*.rs.bk

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python 视觉识别微服务（模型文件过大不入库，由 export_model.py / download_models.py 生成）
python/models/*.onnx
python/models/*.pt
python/models/*.pth.tar
python/models/*.zip
python/models/categories_places365.txt
# 运行态：用户当前选择的语义模型档位（POST /model 切换时写入，随机器不同）
python/models/current_clip.json
# 运行态：用户选择的 CPU 线程数（⚙ 性能设置里可改，随机器不同）
python/models/current_threads.json
# 旧版遗留运行态（v5 已不写；保留忽略以免历史文件误入库）
python/models/current_cls.json
# 语义模型（Chinese-CLIP 子目录，共 >1.9GB：应用内下载 / download_models.py 获取，不入库）
python/models/chinese-clip/
python/models/chinese-clip-fp32/
python/models/chinese-clip-l14/
python/models/chinese-clip-l14-224/
python/models/chinese-clip-vit-b-16/
python/models/current_clip_gpu.json
python/data/
python/__pycache__/
python/vcr/**/__pycache__/
*.pyc

# 本地地理数据下载缓存（python/download_geo_data.py 的原始 JSON 缓存，可随时重下）
python/.geo_cache/

# 根目录模型文件（与 python/models/*.pt 同理，过大不入库）
python/*.pt

# Python 视觉微服务打包产物（PyInstaller 生成，体积过大不入库；由 build_vcr_exe.ps1 重建）
python/.venv-vcr/
python/build/
python/dist/
python/staging/

# 早期本地地理实验（experiments/local_geocode，已被 python/download_geo_data.py + geo_index 取代）
experiments/

# 开发环境搭建脚本与安装器（不入库）
setup-env.ps1
manual-install-rust.md
rustup-init.exe
nul

# 语义搜索测试产物（模型向量矩阵 23MB 可随时重跑 embed_all.py 重建；下载日志无价值）
python/bench/out/
python/bench/*_log.txt

# Git 损坏现场备份（2026-09-17 保留用于对账排查，内含残缺 .git，禁止入库）
.git.broken-*/
