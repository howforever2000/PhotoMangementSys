[package]
name = "photo-management-sys"
version = "0.1.0"
description = "Local photo album management app"
authors = ["haoyuan"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "photo_management_sys_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["protocol-asset"] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
# 图片解码/缩略图生成。支持常见图片格式，jpeg/png/webp 等由 feature 控制
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "webp", "gif", "bmp"] }
# JPEG DCT 降采样解码（scale 参数只解所需 DCT 块，生成缩略图比全尺寸解码快 16~64 倍）
# 修复：image::open 全尺寸解码 6000x4000 大图需 5~14 秒，前端换封面卡顿
jpeg-decoder = "0.3"
# 遍历目录找图片
walkdir = "2"
# 读取图片 EXIF（拍摄时间）
kamadak-exif = "0.6"

