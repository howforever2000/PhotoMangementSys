# 视觉内容识别微服务依赖（模型导出仅需一次，推理期只需以下运行时依赖）
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
fastapi>=0.110
uvicorn>=0.29
# 创意工坊微服务 python-studio/server.py 的 multipart 上传（图片 + 蒙版）依赖；
# 缺了它在路由注册阶段就报 RuntimeError，服务起不来（连 /health 都不可达）
python-multipart>=0.0.9
# 方案 A：默认用 DirectML 版（与打包口径一致，见 requirements-vcr-packaging.txt）。
# 它 import 名仍是 onnxruntime，多出 DmlExecutionProvider（DX12，N/A/I 显卡通用）。
# 只需纯 CPU 环境（无显卡/低配机器调试）时改回：onnxruntime>=1.17
onnxruntime-directml>=1.17
Pillow>=10.0
numpy>=1.26
# 人脸检测/对齐（SCRFD + ArcFace 推理期必需）
opencv-python>=4.8
# 语义搜索：tokenizer.json 加载 + CLIP 双塔拆图（一次性，无 heavy 依赖）
tokenizers>=0.19
onnx>=1.15
