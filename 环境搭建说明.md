# 环境搭建说明

> 生成日期：2026-07-30
> 状态：基础框架环境已就绪 ✅（尚未开始业务代码编写）

## 一、本机开发环境（已验证）

| 依赖 | 版本 | 说明 |
|:---|:---|:---|
| Node.js | v24.18.0 | 前端构建 |
| npm | 11.18.0 | 包管理 |
| Rust | 1.97.1 (stable-x86_64-pc-windows-msvc) | Tauri 后端 |
| Visual Studio 2022 Community | 已安装 | MSVC 链接器（Tauri Windows 必需） |
| WebView2 Runtime | 150.0.4078.105 | Tauri 窗口渲染（Windows 必需） |

已配置的镜像加速：
- `C:\Users\haoyuan\.cargo\config.toml`：crates.io 走 rsproxy.cn 国内镜像

## 二、项目框架结构

```
PhotoMangementSys/
├── index.html                 # Vite 入口
├── package.json               # 前端依赖与脚本
├── vite.config.ts             # Vite 配置（含 Tauri 适配）
├── tsconfig.json              # TS 配置
├── src/                       # Vue3 + TS 前端
│   ├── main.ts / App.vue      # 入口（脚手架默认页，待替换）
│   ├── router/                # Vue Router（待实现，见需求 §五）
│   ├── stores/                # Pinia 状态（待实现，见需求 §3.2）
│   ├── views/                 # 页面：相册列表 / 相册详情（待实现）
│   └── types/                 # Album 等类型定义（待实现，见需求 §3.1）
├── src-tauri/                 # Rust 后端
│   ├── Cargo.toml             # 已含 rusqlite(bundled)、tauri-plugin-dialog
│   ├── tauri.conf.json        # 窗口 1280x800，标题「本地相册管理」
│   ├── capabilities/default.json  # 已授权 dialog（文件夹/图片选择、确认框）
│   └── src/
│       ├── main.rs / lib.rs   # 入口（已注册 dialog 插件）
│       └── db/                # SQLite 模块（待实现，见需求 §2.4）
├── 需求文档.md                 # 整体技术架构
├── 主框架需求分析.md            # 主框架功能/接口/验收标准
└── 环境搭建说明.md（本文档）
```

## 三、已安装的关键依赖

**前端**（package.json）：
- `vue@3.5` / `vue-router@4` / `pinia@2`
- `@tauri-apps/api@2` / `@tauri-apps/plugin-dialog@2` / `@tauri-apps/plugin-opener@2`
- 开发：`vite@6`、`typescript@5.6`、`vue-tsc`、`@tauri-apps/cli@2`

**Rust**（Cargo.toml）：
- `tauri@2`、`tauri-plugin-dialog`、`tauri-plugin-opener`
- `rusqlite@0.32`（bundled 特性，自带 SQLite，无需外部安装）
- `serde` / `serde_json`

> Python 图像处理微服务（FastAPI + Nuitka）属于后续阶段，本次框架未涉及，
> 届时需要独立 Python 环境，不影响当前主框架开发。

## 四、常用命令

```bash
npm run tauri dev     # 开发模式（前端热更新 + 桌面窗口）
npm run build         # 前端类型检查 + 构建
npm run tauri build   # 打包发布安装包
```

## 五、注意事项

1. ⚠️ 若终端提示 `vue-tsc 不是内部或外部命令` / devDependencies 未安装，
   说明 shell 中存在 `NODE_ENV=production`，请先执行：
   - PowerShell：`$env:NODE_ENV="development"`
   - CMD：`set NODE_ENV=development`
   然后再运行 npm 命令。
2. 首次 `npm run tauri dev` 会完整编译 Rust 依赖，耗时约 3~10 分钟，之后增量编译很快。
3. 数据库文件建议存放于 Tauri 的 `app_data_dir`（即 `%APPDATA%/com.haoyuan.photo-management-sys/`），
   实现数据库初始化模块时使用 `app.path().app_data_dir()` 获取。

## 六、GPU 加速（DirectML，方案 A）

**运行时选型**：`onnxruntime-directml==1.24.4`（见 `python/requirements-vcr-packaging.txt`）。
它是 `onnxruntime` 的**同名平替**（import 名仍是 `onnxruntime`），多带一个
`DmlExecutionProvider`（走 DirectX 12），NVIDIA / AMD / Intel 全系显卡通用，用户无需装 CUDA/cuDNN。

- ❌ 不要装 `onnxruntime-gpu`（CUDA 版）：只认 NVIDIA，AMD/核显机器装了也用不上，且需匹配 CUDA/cuDNN。
- ⚠️ 三个包同名互斥（`onnxruntime` / `onnxruntime-gpu` / `onnxruntime-directml`），切换前必须先卸载原包。
- 版本仍 pin `1.24.4`：与 CPU 版口径一致，规避 1.25+ 对 CLIP fp16 塔 CPU 慢 2.9× 的坑（BUG-2026-0921-002）。

**开发版**：识别服务跑在 `python/.venv-vcr`（Rust `vision.rs::python_candidates()` 的第一个候选），
不是系统 base 环境 —— 在 base 里装 GPU 包对 app 无效。切换方式：

```powershell
.\python\.venv-vcr\Scripts\python.exe -m pip uninstall -y onnxruntime
.\python\.venv-vcr\Scripts\python.exe -m pip install onnxruntime-directml==1.24.4 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**发布版**：运行时在构建期打进 `python/dist/vcr-server.exe`（PyInstaller），用户无法自行 pip 安装 →
改完依赖必须重构建：`powershell -ExecutionPolicy Bypass -File python\build_vcr_exe.ps1`（实测 98.7MB）。
冻结产物可用 `experiments/verify_gpu_exe.ps1` 自测：拉起 exe → `/gpu` 应返回
`provider: DmlExecutionProvider`，且 `sessions.det` 实测绑定 DML。

**生效范围与开关**：默认强制 CPU（`_gpu_forced_off=True`），需在「性能设置」点「🚀 启用加速」；
GPU 对**人物检测 / 人脸 / OCR** 生效；CLIP **fp16 档固定 CPU**（AMD DML 对 fp16 图有算子级数值 bug），
想让语义索引也走 GPU 需下载 B/16 **fp32** 档。
