# 环境搭建说明

> 生成日期：2026-07-30
> 状态：基础框架环境已就绪 ✅（尚未开始业务代码编写）

## 一、本机开发环境（已验证）

| 依赖 | 版本 | 说明 |
|:---|:---|:---|
| Node.js | v24.18.0 | 前端构建 |
| npm | 11.18.0 | 包管理 |
| Rust | 1.97.1 (stable-x86_64-pc-windows-msvc) | Tauri 后端 |
| Visual Studio 2022 Community | 已安装 | MSVC 链接器（Tauri Windows 必需） |
| WebView2 Runtime | 150.0.4078.105 | Tauri 窗口渲染（Windows 必需） |

已配置的镜像加速：
- `C:\Users\haoyuan\.cargo\config.toml`：crates.io 走 rsproxy.cn 国内镜像

## 二、项目框架结构

```
PhotoMangementSys/
├── index.html                 # Vite 入口
├── package.json               # 前端依赖与脚本
├── vite.config.ts             # Vite 配置（含 Tauri 适配）
├── tsconfig.json              # TS 配置
├── src/                       # Vue3 + TS 前端
│   ├── main.ts / App.vue      # 入口（脚手架默认页，待替换）
│   ├── router/                # Vue Router（待实现，见需求 §五）
│   ├── stores/                # Pinia 状态（待实现，见需求 §3.2）
│   ├── views/                 # 页面：相册列表 / 相册详情（待实现）
│   └── types/                 # Album 等类型定义（待实现，见需求 §3.1）
├── src-tauri/                 # Rust 后端
│   ├── Cargo.toml             # 已含 rusqlite(bundled)、tauri-plugin-dialog
│   ├── tauri.conf.json        # 窗口 1280x800，标题「本地相册管理」
│   ├── capabilities/default.json  # 已授权 dialog（文件夹/图片选择、确认框）
│   └── src/
│       ├── main.rs / lib.rs   # 入口（已注册 dialog 插件）
│       └── db/                # SQLite 模块（待实现，见需求 §2.4）
├── 需求文档.md                 # 整体技术架构
├── 主框架需求分析.md            # 主框架功能/接口/验收标准
└── 环境搭建说明.md（本文档）
```

## 三、已安装的关键依赖

**前端**（package.json）：
- `vue@3.5` / `vue-router@4` / `pinia@2`
- `@tauri-apps/api@2` / `@tauri-apps/plugin-dialog@2` / `@tauri-apps/plugin-opener@2`
- 开发：`vite@6`、`typescript@5.6`、`vue-tsc`、`@tauri-apps/cli@2`

**Rust**（Cargo.toml）：
- `tauri@2`、`tauri-plugin-dialog`、`tauri-plugin-opener`
- `rusqlite@0.32`（bundled 特性，自带 SQLite，无需外部安装）
- `serde` / `serde_json`

> Python 图像处理微服务（FastAPI + Nuitka）属于后续阶段，本次框架未涉及，
> 届时需要独立 Python 环境，不影响当前主框架开发。

## 四、常用命令

```bash
npm run tauri dev     # 开发模式（前端热更新 + 桌面窗口）
npm run build         # 前端类型检查 + 构建
npm run tauri build   # 打包发布安装包
```

## 五、注意事项

1. ⚠️ 若终端提示 `vue-tsc 不是内部或外部命令` / devDependencies 未安装，
   说明 shell 中存在 `NODE_ENV=production`，请先执行：
   - PowerShell：`$env:NODE_ENV="development"`
   - CMD：`set NODE_ENV=development`
   然后再运行 npm 命令。
2. 首次 `npm run tauri dev` 会完整编译 Rust 依赖，耗时约 3~10 分钟，之后增量编译很快。
3. 数据库文件建议存放于 Tauri 的 `app_data_dir`（即 `%APPDATA%/com.haoyuan.photo-management-sys/`），
   实现数据库初始化模块时使用 `app.path().app_data_dir()` 获取。
