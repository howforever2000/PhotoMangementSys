# 本地相册管理软件 - 技术方案

## 架构概览

本方案采用 **Tauri (Rust) + Vue3 + TypeScript** 作为主应用框架，将图像处理算法单元拆分为独立的 **Python 微服务**（打包为单一可执行文件），实现前后端分离、模块解耦的桌面应用架构。

```
┌──────────────────────────────────────────┐
│                用户界面层                │
│         Vue3 + TypeScript + Vite         │
│  (时间线、大图查看、相册管理、标签编辑)  │
└──────────────┬───────────────────────────┘
               │ invoke (Tauri IPC)
┌──────────────▼───────────────────────────┐
│              Tauri 核心层                │
│            Rust (原生二进制)             │
│  • 文件扫描 & 增量索引                   │
│  • SQLite 元数据管理                     │
│  • 缩略图异步生成 (image-rs)             │
│  • 系统级调用 (文件监控、窗口管理)        │
│  • 微服务生命周期管理                    │
└───────┬───────────────────┬──────────────┘
        │                   │
        │ 文件系统           │ HTTP (localhost)
        │ 数据库             │
┌───────▼───────────┐  ┌───▼────────────────┐
│  本地文件 & DB    │  │ Python 图像处理服务 │
│  • 用户图片目录   │  │  (独立exe)          │
│  • 缓存缩略图     │  │  FastAPI + numpy    │
│  • SQLite 元数据  │  │  + scikit-image     │
└──────────────────┘  └────────────────────┘
```

**关键优势**：
- 主程序体积极小（~20MB），启动极快（<1s）
- 图像处理模块按需启动，不占用常驻资源
- 前端享受现代 Web 生态，UI 开发高效
- 图像算法利用 Python 丰富生态，开发便捷

---

## 技术栈详表

| 层级 | 技术选型 | 版本 / 说明 |
|:---|:---|:---|
| **桌面框架** | Tauri 2.x | Rust 驱动的轻量桌面壳 |
| **UI 框架** | Vue 3 + TypeScript | 组合式 API + 强类型 |
| **构建工具** | Vite | 前端开发与打包 |
| **Rust 侧核心依赖** | `rusqlite`, `image`, `notify`, `walkdir`, `serde` | 数据库、图像缩略图、文件监控、序列化 |
| **Python 服务框架** | FastAPI | 高性能异步 REST API |
| **图像处理库** | `numpy`, `scikit-image`, `opencv-python-headless` | 直方图均衡化、区域 CLAHE 等 |
| **Python 打包** | Nuitka (主推) / PyInstaller | 编译为独立可执行文件 |
| **通信协议** | HTTP REST + JSON | 轻量、跨语言通用 |

---

## 模块职责

### 1. 主应用（Tauri + Vue）

**Rust 侧（`src-tauri`）**
- 文件系统遍历与增量索引（`walkdir` + `notify`）
- SQLite 数据库建表与查询（`rusqlite`）
- 缩略图生成引擎：多线程异步生成 256px 缩略图，缓存至 `%APPDATA%` 目录
- 路径重定向与破图修复
- **微服务管理**：启动/停止 Python 服务，检测端口，心跳监控
- 提供 Tauri 命令供前端调用（扫描、查询、标签、服务调用等）

**Vue3 侧（`src`）**
- 时间线视图、虚拟滚动网格（`vue-virtual-scroller`）
- 大图查看器（鼠标缩放、拖拽）
- EXIF 信息面板
- 相册 CRUD、标签评分、搜索过滤
- **图像处理入口**：调用 Tauri 命令触发 Python 服务，展示处理结果

### 2. Python 图像处理微服务

- 独立的 FastAPI 应用，打包为 `image_processor.exe`
- 接收图片路径与算法参数，处理后写出结果，返回结果路径
- 可随时扩展新算法，不影响主程序
- 仅在用户调用图像处理功能时被主程序唤起，处理完成后空闲一段时间自动退出（节省内存）

---

## 通信流程

### 处理请求流程

```
[用户在大图模式点击“直方图均衡化”]
        │
        ▼
[Vue] invoke('call_image_process', { imagePath, algorithm, params })
        │
        ▼
[Rust Command] 
  1. 检查 image_processor.exe 是否存活，若未启动则启动之
  2. 获取本地端口号（动态绑定）
  3. POST http://localhost:{port}/process
     Body: { "image_path": "C:\\...", "algorithm": "clahe", 
             "params": { "clip_limit": 2.0, "tile_size": 8 } }
        │
        ▼
[Python FastAPI]
  1. 读取原图（按路径）
  2. 根据 algorithm 调用对应处理函数
  3. 生成结果图，写入用户图片同目录（命名规则：原图名_processed_时间戳.jpg）
  4. 返回 { "status": "ok", "output_path": "C:\\...result.jpg" }
        │
        ▼
[Rust] 收到响应，将结果路径返回前端；前端在预览区展示结果图
```

### 微服务生命周期管理

- **启动**：首次调用时由 Rust 使用 `std::process::Command` 启动 `image_processor.exe`，传递一个 `--port 0` 参数让 OS 自动分配端口。Rust 读取进程 stdout 第一行获得实际端口。
- **空闲退出**：Python 服务内置定时器，若连续 5 分钟无请求，自动 `sys.exit(0)` 退出。
- **关闭**：Tauri 应用退出时，Rust 的 `on_exit` 钩子主动 kill 该子进程（若存活）。
- **崩溃恢复**：Rust 侧在请求失败时检测进程存活，若已死则自动重启一次，并返回友好错误给前端。

---

## 部署与分发

### 构建输出结构
```
release/
├── MyPhotoApp.exe          # Tauri 生成的主程序
├── image_processor.exe     # Nuitka 编译的 Python 服务
└── ... (其他资源)
```

### 打包流程
1. **前端构建**：`npm run build` 生成 `dist/`
2. **Python 编译**：
   ```bash
   nuitka --standalone --windows-console-mode=disable --enable-plugin=upx \
          --include-package=fastapi --include-package=uvicorn \
          -o image_processor.exe main.py
   ```
   优化体积与启动速度，最终约 180MB 左右。
3. **Tauri 打包**：将 `image_processor.exe` 放入 `src-tauri/binaries/`（Tauri 2.0 的资源目录），执行 `cargo tauri build`，生成体积约 200MB 的安装包。

### 用户环境
- Windows 10/11 64 位
- 无需安装任何 Python、Node.js 或额外运行时
- 安装包双击即用，完全绿色（或标准 MSI 安装）

---

## 容错与异常处理

| 场景 | 处理策略 |
|:---|:---|
| 微服务启动失败 | Rust 捕获启动错误，返回错误信息给前端，主功能不受影响 |
| 微服务处理超时 | HTTP 请求设置 30 秒超时，超时则杀死进程并提示用户 |
| 微服务运行中崩溃 | Rust 检测到连接拒绝或管道断裂，自动重启一次并重试 |
| 端口冲突 | Python 监听端口 0（随机），由 Rust 解析，无冲突可能 |
| 图片路径无效 | 微服务返回 400 错误，前端提示“文件不存在或已移动” |
| 主程序关闭时微服务仍在运行 | Tauri `on_exit` 强制杀进程，确保无残留 |

---

## 扩展性

- **新增图像算法**：仅需在 Python 服务中添加新路由和处理函数，不影响主程序。
- **未来移植到其他平台**：Tauri 跨平台，Rust 代码基本不变；Python 服务亦可跨平台运行。
- **性能优化**：若后续算法需求增多，可将 Python 服务升级为常驻服务，或逐步将成熟算法用 Rust 重写，移除此依赖。

---

## 总结

本方案以 **Tauri 保证核心体验（轻量、快速、原生）**，以 **独立 Python 微服务满足算法灵活性与开发效率**，通过清晰的进程间通信达成解耦，是个人开发者实现“轻量级本地相册 + 创新图像算法”的理想技术路线。