# 本地相册管理软件 - 主框架需求分析

> **版本**：v1.0  
> **目标**：搭建可运行的基础框架，实现「相册管理」核心功能  
> **技术栈**：Tauri 2.x + Vue3 + TypeScript + SQLite  
> **范围**：不包含图片扫描、缩略图生成、AI 处理等进阶功能

---

## 一、总体目标

搭建相册管理的主框架，实现用户对本地图片文件夹的「抽象化管理」。用户通过软件创建相册，绑定本地实际文件夹路径，并为相册添加名称、简介、封面等元数据。软件负责持久化这些元数据，并提供列表浏览与详情查看能力。

**核心原则**：
- 相册是对本地文件夹的「引用」，不移动、不复制用户原始文件
- 元数据（名称、简介、封面）与原始文件分离存储
- 框架设计预留后续图片扫描、标签、AI 处理的扩展接口

---

## 二、功能模块划分

### 2.1 相册列表模块

**功能描述**：展示用户已创建的所有相册，以卡片网格形式呈现。

**包含子功能**：

| 子功能 | 优先级 | 说明 |
|:---|:---|:---|
| 相册卡片展示 | P0 | 每张卡片显示封面图（如有）、相册名称、简介摘要、文件夹路径 |
| 新建相册入口 | P0 | 提供明显的创建按钮，唤起创建流程 |
| 空状态引导 | P0 | 无相册时展示引导界面，提示用户创建 |
| 相册排序 | P1 | 默认按更新时间倒序，后续支持按名称/创建时间排序 |
| 搜索过滤 | P2 | 按相册名称关键词过滤（基础框架可暂不实现） |

**交互定义**：
- 点击任意相册卡片 → 跳转至该相册的详情页
- 卡片悬停时显示轻微上浮阴影，提示可点击
- 路径过长时截断显示，完整路径通过 Tooltip 展示

---

### 2.2 相册创建模块

**功能描述**：引导用户选择本地文件夹，并填写相册元数据，完成相册创建。

**包含子功能**：

| 子功能 | 优先级 | 说明 |
|:---|:---|:---|
| 系统文件夹选择 | P0 | 调用操作系统原生文件夹对话框，用户选择本地磁盘上的任意目录 |
| 相册名称填写 | P0 | 必填，默认自动填充为所选文件夹的名称 |
| 相册简介填写 | P1 | 选填，多行文本，用于描述相册内容 |
| 路径合法性校验 | P0 | 校验路径是否存在、是否已被其他相册引用 |
| 创建确认 | P0 | 点击创建后写入数据库，刷新列表 |

**交互定义**：
- 文件夹选择对话框支持浏览整个文件系统（Windows: 盘符 → 目录树；macOS/Linux: 标准文件选择器）
- 若路径已被其他相册使用，提示「该文件夹已被相册 XXX 引用，是否继续？」
- 创建成功后自动关闭对话框，新相册出现在列表首位

---

### 2.3 相册详情模块

**功能描述**：展示单个相册的完整信息，并提供编辑与操作入口。

**包含子功能**：

| 子功能 | 优先级 | 说明 |
|:---|:---|:---|
| 相册信息展示 | P0 | 显示名称、简介、实际路径、创建时间、更新时间 |
| 封面展示 | P0 | 大图展示当前封面，无封面时显示占位提示 |
| 编辑信息 | P0 | 修改相册名称和简介 |
| 设置封面 | P0 | 从本地选择一张图片作为该相册的封面 |
| 删除相册 | P0 | 从软件中移除该相册记录（不删除本地文件） |
| 返回导航 | P0 | 返回相册列表页 |
| 图片浏览占位 | P1 | 预留图片网格区域，当前版本仅展示提示信息 |

**交互定义**：
- 封面区域点击后唤起系统文件选择对话框，过滤图片格式（jpg/jpeg/png/webp）
- 设置封面后即时刷新显示
- 删除操作需二次确认，明确告知「仅删除记录，不删除本地文件」
- 编辑信息通过对话框完成，保存后即时刷新

---

### 2.4 数据持久化模块（后端）

**功能描述**：负责相册元数据的存储、查询、更新与删除。

**包含子功能**：

| 子功能 | 优先级 | 说明 |
|:---|:---|:---|
| 数据库初始化 | P0 | 应用启动时自动检测并创建 SQLite 数据库及表结构 |
| 相册创建 | P0 | 接收名称、路径、简介，写入数据库 |
| 相册列表查询 | P0 | 返回所有相册，按更新时间倒序 |
| 相册详情查询 | P0 | 根据相册 ID 返回完整信息 |
| 相册更新 | P0 | 支持更新名称、简介、封面路径 |
| 相册删除 | P0 | 根据 ID 删除记录 |
| 路径唯一性校验 | P0 | 创建时检查路径是否已存在 |

---

## 三、数据模型定义

### 3.1 相册实体（Album）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 唯一标识 |
| name | TEXT | NOT NULL | 相册显示名称 |
| path | TEXT | NOT NULL, UNIQUE | 绑定的本地文件夹绝对路径 |
| description | TEXT | 可空 | 相册简介/描述 |
| cover_path | TEXT | 可空 | 封面图片的绝对路径 |
| created_at | INTEGER | NOT NULL | 创建时间戳（Unix 秒） |
| updated_at | INTEGER | NOT NULL | 最后更新时间戳（Unix 秒） |

**约束说明**：
- `path` 的唯一性约束确保同一文件夹不能被重复创建为相册
- `cover_path` 指向的文件应位于 `path` 目录或其子目录下（逻辑约束，数据库层面不强制）
- 所有时间戳使用 Unix 时间（秒级），便于前后端传输与排序

### 3.2 前端状态定义

**全局状态（Pinia Store）**：

| 状态项 | 类型 | 说明 |
|:---|:---|:---|
| albums | Album[] | 当前相册列表缓存 |
| currentAlbum | Album \| null | 当前正在查看的相册详情 |
| isLoading | boolean | 全局加载状态 |

**路由参数**：

| 参数 | 类型 | 说明 |
|:---|:---|:---|
| id | string | 相册详情页路由参数，对应相册的 id 字段 |

---

## 四、前后端接口定义

### 4.1 接口概览

| 接口标识 | 方向 | 功能 | 输入 | 输出 |
|:---|:---|:---|:---|:---|
| create_album | 前端 → 后端 | 创建相册 | 名称、路径、简介 | 完整相册对象（含生成 ID） |
| get_albums | 前端 → 后端 | 获取相册列表 | 无 | 相册对象数组 |
| get_album | 前端 → 后端 | 获取单个相册 | 相册 ID | 完整相册对象 |
| update_album | 前端 → 后端 | 更新相册信息 | 相册 ID + 变更字段 | 无 |
| delete_album | 前端 → 后端 | 删除相册 | 相册 ID | 无 |

### 4.2 接口详细定义

#### create_album

- **输入结构**：
  - name: string（相册名称，必填，1-100 字符）
  - path: string（文件夹绝对路径，必填）
  - description: string?（简介，选填，最多 500 字符）
- **输出结构**：完整 Album 对象（含后端生成的 id、created_at、updated_at）
- **错误场景**：
  - 名称不能为空或超长
  - 路径不存在或不可访问
  - 路径已被其他相册引用（唯一性冲突）

#### get_albums

- **输入**：无参数
- **输出结构**：Album[] 数组
- **排序规则**：按 updated_at 降序（最近更新的排在最前）
- **错误场景**：数据库连接失败

#### get_album

- **输入**：id: number（相册 ID）
- **输出结构**：完整 Album 对象
- **错误场景**：ID 不存在

#### update_album

- **输入结构**：
  - id: number（必填）
  - name: string?（可选，提供则更新）
  - description: string?（可选，提供则更新）
  - cover_path: string?（可选，提供则更新）
- **输出**：无
- **副作用**：自动更新 updated_at 为当前时间
- **错误场景**：ID 不存在；name 超长

#### delete_album

- **输入**：id: number
- **输出**：无
- **副作用**：级联删除该相册的所有元数据记录（当前版本无其他关联表，后续若有图片索引记录需一并清理）
- **错误场景**：ID 不存在

---

## 五、页面结构与导航

### 5.1 页面层级

```
App
├── /albums          相册列表页（默认首页）
│   └── 新建相册对话框（模态）
└── /album/:id       相册详情页
    ├── 编辑信息对话框（模态）
    └── 删除确认对话框（模态）
```

### 5.2 导航规则

| 操作 | 来源页面 | 目标页面 | 方式 |
|:---|:---|:---|:---|
| 应用启动 | — | /albums | 默认路由重定向 |
| 点击相册卡片 | /albums | /album/:id | 编程式导航（router.push） |
| 点击返回按钮 | /album/:id | /albums | 编程式导航（router.back） |
| 删除相册后 | /album/:id | /albums | 自动跳转 |

### 5.3 页面布局规范

**相册列表页（/albums）**：
- 顶部工具栏：左侧标题「我的相册」，右侧「新建相册」主按钮
- 主体区域：响应式卡片网格（最小 280px，自动填充）
- 空状态：居中展示，含引导文案和创建按钮

**相册详情页（/album/:id）**：
- 顶部导航栏：左侧「返回相册列表」文字按钮，右侧「编辑信息」「删除相册」操作按钮
- 信息头区域：左侧封面大图（可点击），右侧名称/路径/时间/简介
- 内容区域：预留图片浏览网格（当前版本展示占位提示）

---

## 六、系统对话框调用规范

### 6.1 文件夹选择对话框

**触发时机**：创建相册时点击「选择」按钮
**对话框配置**：
- 类型：文件夹选择（directory = true）
- 多选：禁用（multiple = false）
- 标题：「选择相册文件夹」
- 默认路径：用户主目录或上次选择路径（如有记忆功能）

**结果处理**：
- 用户确认选择后，将绝对路径回填至表单
- 若相册名称为空，自动将文件夹名称填充为默认相册名

### 6.2 图片选择对话框（封面设置）

**触发时机**：详情页点击封面区域
**对话框配置**：
- 类型：文件选择（directory = false）
- 多选：禁用
- 文件过滤：仅显示图片格式（jpg, jpeg, png, webp）
- 标题：「选择封面图片」
- 默认路径：当前相册绑定的文件夹路径

**结果处理**：
- 用户确认后，将图片绝对路径发送至后端更新 cover_path
- 前端即时刷新封面显示

---

## 七、错误处理与边界场景

### 7.1 前端错误处理

| 场景 | 用户提示 | 行为 |
|:---|:---|:---|
| 创建相册时名称未填 | 提示「请填写相册名称」 | 阻止提交，聚焦输入框 |
| 创建相册时路径未选 | 提示「请选择文件夹」 | 阻止提交 |
| 创建时路径已被占用 | 提示「该文件夹已被相册『XXX』使用」 | 阻止提交 |
| 获取列表失败 | 提示「获取相册列表失败」 | 展示空状态或重试按钮 |
| 获取详情失败 | 提示「相册不存在或已删除」 | 自动返回列表页 |
| 更新/删除失败 | 提示具体操作失败信息 | 保持当前页面，允许重试 |

### 7.2 后端错误处理

| 场景 | 返回策略 | 日志 |
|:---|:---|:---|
| 数据库连接失败 | 返回 500 等效错误信息 | 记录错误详情 |
| 路径唯一性冲突 | 返回明确冲突信息（含冲突相册名） | 记录尝试路径 |
| 路径不存在 | 返回路径无效错误 | 记录路径 |
| ID 不存在 | 返回资源不存在错误 | 记录查询 ID |

### 7.3 边界场景

| 场景 | 预期行为 |
|:---|:---|
| 用户删除了本地文件夹，但相册记录仍在 | 相册记录保留，封面/路径显示为失效状态（后续版本标记为「路径失效」） |
| 用户重命名了本地文件夹 | 相册记录中的 path 不会自动更新，保持原值（后续版本可增加路径监控修复） |
| 封面图片被用户删除 | 详情页加载封面失败时，回退显示占位图 |
| 相册名称为空字符串 | 前后端均校验，不允许提交 |
| 简介超长（>500 字符） | 前端输入框限制，后端再次校验 |

---

## 八、验收标准

### 8.1 功能验收

- [ ] 用户可以成功创建相册，绑定本地文件夹，填写名称和简介
- [ ] 相册列表页正确展示所有相册，按更新时间倒序排列
- [ ] 点击相册卡片正确跳转至详情页，展示完整信息
- [ ] 详情页可以修改相册名称和简介，保存后即时生效
- [ ] 详情页可以点击封面区域选择图片，设置后即时显示
- [ ] 删除相册需二次确认，删除后返回列表页且该相册消失
- [ ] 同一文件夹不能被重复创建为相册
- [ ] 应用重启后，已创建的相册数据不丢失

### 8.2 体验验收

- [ ] 应用冷启动时间 < 2 秒
- [ ] 文件夹选择对话框为系统原生样式
- [ ] 列表页空状态有明确的引导操作
- [ ] 所有操作有加载状态或即时反馈
- [ ] 错误提示使用中文，语义明确

### 8.3 稳定性验收

- [ ] 连续创建 50 个相册无异常
- [ ] 快速切换列表页和详情页无数据错乱
- [ ] 删除操作后数据库无残留记录
- [ ] 异常关闭应用后数据不损坏

---

## 九、后续扩展预留

当前框架在设计和实现上需预留以下扩展点，避免后续重构：

| 扩展方向 | 预留措施 |
|:---|:---|
| **图片扫描索引** | albums 表已包含 path 字段，后续可基于此路径遍历文件，建立 photos 关联表 |
| **缩略图系统** | 封面路径使用绝对路径，后续缩略图可统一放在应用缓存目录，通过映射表关联 |
| **标签系统** | 后续增加 tags 表和 album_tags / photo_tags 关联表，不影响现有结构 |
| **AI 图像处理** | 前端预留处理按钮入口，后端命令接口可独立扩展，不与相册管理耦合 |
| **路径变更修复** | 后续增加文件监控（watch）或手动「修复路径」功能，可更新 album.path 字段 |
| **多设备同步** | 数据库使用 SQLite，后续可导出/导入为 JSON，或迁移至服务端数据库 |

---

## 十、附录

### 10.1 术语表

| 术语 | 定义 |
|:---|:---|
| 相册（Album） | 软件中对本地文件夹的抽象管理单元，包含元数据（名称、简介、封面） |
| 实际地址 / 路径（Path） | 本地磁盘上的文件夹绝对路径，如 `D:\旅行照片` 或 `/Users/xxx/Photos` |
| 封面（Cover） | 用户为相册指定的一张代表性图片，用于列表页展示 |
| 元数据 | 与原始图片文件分离存储的描述信息（名称、简介、封面路径等） |

### 10.2 相关文档索引

| 文档 | 说明 |
|:---|:---|
| 技术方案.md | 整体技术架构与选型说明 |
| 基础框架搭建说明.md | 具体代码实现与搭建步骤 |
| 主框架需求分析.md（本文档） | 功能定义、数据模型、接口规范、验收标准 |
