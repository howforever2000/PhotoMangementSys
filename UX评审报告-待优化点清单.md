# PhotoManagementSys v0.3.1 交互体验评审 · 待优化点清单

> 评审方式：以真实用户视角驱动应用走查（无头浏览器 + 真实数据库 653 张照片 / 12 相册 / 170 人物聚类），并对照源码与数据验证。
> 日期：2026-09-17

---

## 一、缺陷类（建议优先修）

### 1. 灯箱组件存在真实的运行时崩溃（TDZ 引用错误）⭐ 实锤
- **现象**：打开照片灯箱时控制台抛出 `ReferenceError: Cannot access 'metaOverrides' before initialization`、`Cannot access 'rating' before initialization`。
- **根因**：`src/components/PhotoLightbox.vue` 中 `meta` computed（L196）读取 `metaOverrides`，但它到 L278 才声明；而 L258 的 `watch(..., { immediate: true })` 在 setup 阶段同步调用 `tryEnsureScanned()`，第一时间读取 `metaOverrides.value[path]`（L281），此时变量尚未初始化（TDZ）。`rating` 同样模式。
- **影响**：从时间线 / 回忆等页面打开灯箱时，扫描状态与信息面板首次展示可能失效，且每次报错。
- **建议**：把 `metaOverrides` / `rating` 的声明移到 watch 之前（或延迟读取）。

### 2. 「图片扫描」页顶部统计"已入库照片"永远是 0 ⭐ 实锤
- **根因**：`src/views/ScanHub.vue` L78 `const scannedIn = ref(0)` 声明后**从未被赋值**（L88–96 的 watch 只更新了 totalPhotos / totalAlbums / scannedAlbums）。
- **现象**：顶部显示"12 个已入库相册 / 0 已入库照片"，与智慧相册页"653 已扫描入库照片"自相矛盾。
- **建议**：`scannedIn.value = list.reduce((n, a) => n + (a.scanned_photo_count || 0), 0)`。

### 3. 渲染引擎异常时应用白屏挂死，无任何提示
- **现象**：本次评审环境中 WebView2 启动即崩溃（Crashpad 转储：`0x80000003 @ msedge.dll`），应用窗口照常创建但永远白屏，用户得不到任何错误信息，只能看着"卡死"。
- **建议**：主窗口增加 WebView2 初始化失败检测 + 明确的降级错误页（副窗口已有 `showFatal` 机制，主窗口反而没有）；至少在文档/常见问题中给出"白屏 = 检查 WebView2 运行时"的排查指引。

---

## 二、功能有效性 / 数据质量

### 4. 「地点」功能全线空转，且没有任何用户引导 ⭐ 重要
- **数据实况**：653/653 张照片 `location = NULL`；相册「按地点」视图显示"0 个相册有地点"、12 个相册全部"📍 未知地点"；智慧相册地点 tab 里 653 张全部归入"未记录地点"；回忆页的地点聚合为空。
- **问题**：功能没产出时，界面只是安静地展示空态，用户不知道"是照片没有 GPS？还是解析失败？还是我没扫这一项？"。
- **建议**：
  - 空态页增加诊断信息：「已扫描照片中 X 张含 GPS（占 Y%），Z 张解析失败」；
  - 导入/扫描完成后若 GPS 占比为 0，主动提示"你的相机可能未开启定位记录"；
  - 支持"手动批量设置地点"的入口更前置（目前藏在批量操作里）。

### 5. 时间线"其他"类泛滥（259/653 = 40%），信息密度低
- **数据实况**：内置分类只有 other/portrait/street/night_scene/document 五类，2024 年 12 月的 94 张照片几乎全是"其他"。
- **问题**：用户自建的 21 个语义分类（聚会合影 109、自然风景 36、日落晚霞 25……）明明能把"其他"拆开，但时间线只显示旧的内置类别，两套分类体系割裂。
- **建议**：时间线/灯箱的类别标签接入语义分类结果；"其他"至少可点击查看语义细分或跳转搜索。

### 6. 智能搜索的关键词路径召回不到语义分类内容
- **验证**：搜索"猫"返回 0 结果，但库里存在"猫咪"语义分类（2 张，用户手动建的）与狗、美食等语义分类——这些内容只存在于语义向量/分类表，关键词索引（content 字段）里没有。
- **建议**：关键词搜索同时召回「语义分类名 / 分类关键词」命中的照片；或在搜索页明示"关键词搜的是内置类别，自建分类请用语义搜索"。

### 7. 人物聚类管理在 170 个未命名对象面前不可操作
- **数据实况**：170 个"人物"聚类，几乎全部显示为 P001/P094 这类自动编号，回忆页"近期人物"直接显示"60 次 P101"——对用户毫无意义。
- **问题**：毕业照 108 张 + 聚会 109 张的聚类分裂是正常现象，但产品只提供逐个"改名 / 合并到其他人物…"，170 个聚类人工处理不现实。
- **建议**：
  - 相似聚类自动配对建议（"P094 与 P016 可能是同一人，合并？"）；
  - 支持批量勾选合并、按相册/日期筛选人物；
  - "近期人物"等展示位优先显示已命名人物，未命名的折叠或聚合。

### 8. 其他数据质量点
- 31 张照片无拍摄时间（时间线如何归位需要明示）；
- 分类置信度最低 0.0、平均 0.53，低置信度结果与高置信度无视觉区分，用户无从判断可信度。

---

## 三、交互 / UI 体验

### 9. 首页与扫描中心被"待开发"占位占据
- 首页 4 大板块中「图像处理（待开发）」独占一卡且带锁；扫描中心的「按年·地点浏览」「重复扫描清理」也是"待开发"。
- 建议：待开发项折叠到次要位置或移除，把首屏留给已完成的能力（时间线 / 回忆 / 搜索值得在首页直接露出）。

### 10. 时间线照片上的跳转按钮"↗"仅 22×22px
- 653 张照片每张挂一个 22px 小按钮，点击目标过小（建议 ≥ 40px 热区），且"↗"含义不明确。

### 11. 相册详情页「点击更换封面」与照片预览混淆
- 点击顶部封面图会直接弹出系统选择框更换封面——想"看大图"的用户会误触发。建议封面更换改为显式按钮。

### 12. 可访问性
- 相册卡片用 `<article>` 且无 `role` / `tabindex`，键盘用户无法 Tab 聚焦进入相册。

### 13. 批量导入失败原因仍偏"原始错误"
- 历史反馈"批量导入会失败几个但不知道为什么"。v0.3.1 已有失败详情对话框（本次验证可弹出 Error 文本），但显示的是原始错误串。建议映射为人话（"文件被占用 / 格式不支持 / 路径含特殊字符 / 源文件已移动"）并保留一键重试。

### 14. 相册分组口径易困惑（小）
- "家人"相册文件路径在 `2026\家人`，但按拍摄时间归入"2025 年"分组——按拍摄时间分树本身合理，但卡片上没有任何解释，建议 hover 或角标说明"按拍摄时间归类"。

### 15. 数据卫生（小）
- "test" 相册（53 张）与正式相册混排；建议提供归档/隐藏机制，或至少在批量管理中易于筛选测试相册。

---

## 四、做得好的地方（保持）

- 搜索空态文案 excellent：明确告知原因并给出两条出路（放宽条件 / 去扫描中心建语义索引）。
- 智慧相册「内容分类」卡片式管理（关键词、匹配度、张数一目了然）+ 语义索引覆盖率提示（644/653）。
- 组合扫描"后台执行、离开页面不中断、可随时停止"的承诺在 UI 上反复明示。
- 相册详情信息密度合适（元数据 / 标签 / 组合扫描 / 缩略图分区折叠）。
- 开发者视角的实时日志、数据与路径副窗口是很强的自查能力。

---

## 五、优先级建议

| 优先级 | 事项 |
| --- | --- |
| P0 | #1 灯箱 TDZ 崩溃、#2 统计为 0、#3 白屏无提示 |
| P1 | #4 地点空态引导、#5"其他"类占比、#6 搜索召回、#7 人物批量管理 |
| P2 | #9–#15 交互细节 |
